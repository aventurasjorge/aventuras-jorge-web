# Base URL of your Railway booking API.
# Local dev: point at your local Railway dev server or a deployed staging URL.
# Production: set this in Netlify's dashboard (Site settings > Environment variables),
# NOT in this file — .env is for local development only.
VITE_API_URL=https://your-app.up.railway.app
